import Wang{{properCase name}} from './src/{{name}}.vue'

Wang{{properCase name}}.install = Vue => {
  Vue.component(Wang{{properCase name}}.name, Wang{{properCase name}})
}

export default Wang{{properCase name}}
